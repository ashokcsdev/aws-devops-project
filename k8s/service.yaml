apiVersion: v1
kind: Service

metadata:
  name: aws-devops-app-service

spec:
  type: LoadBalancer

  selector:
    app: aws-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
